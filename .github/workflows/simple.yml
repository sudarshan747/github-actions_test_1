name: shell commands 

on: [push]

jobs:
  run-shell-command:
      runs-on: ubuntu-latest
      steps:
        - name: echo a string
          run: echo "Hello world from, github actions"
        - name: multiline script 
          run: |
            node -v
            python3 -v
            npm -v